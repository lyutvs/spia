// Auto-generated by SPIA — do not edit manually.

/* ──────────── Error Types ──────────── */

export class ApiError<T = unknown> extends Error {
  constructor(public status: number, public data: T, message?: string) { super(message); }
}

export class ApiTimeoutError extends Error {
  constructor(message?: string) { super(message ?? 'Request timed out'); }
}

/* ──────────── DTO Types ──────────── */

export type OrderId = number & { readonly __brand: 'OrderId' };
export const OrderId = (raw: number): OrderId => raw as OrderId;

export type UserId = string & { readonly __brand: 'UserId' };
export const UserId = (raw: string): UserId => raw as UserId;

export interface Circle {
  radius: number;
}
export interface Square {
  side: number;
}
export interface Triangle {
  base: number;
  height: number;
}
export type Shape = ({ kind: 'circle' } & Circle) | ({ kind: 'square' } & Square) | ({ kind: 'triangle' } & Triangle);

export interface Address {
  street: string;
  city: string;
  zipCode: string;
}

export interface BadRequestErrorDto {
  message: string;
  field: string | null;
}

export interface CreateUserRequest {
  name: string;
  email: string;
}

export interface EcAuthRetryResponse {
  message: string;
  authenticated: boolean;
}

export interface EcJavaDto {
  name: string;
  score: number;
}

export interface EcPageableItem {
  id: number;
  name: string;
}

export interface EcTimeoutResponse {
  message: string;
  delayMs: number;
}

export interface EcValidationRequest {
  /**
   * @minLength 1
   * @maxLength 50
   */
  name: string;
  /**
   * @minimum 0
   * @maximum 120
   */
  age: number;
  /**
   * @pattern ^[A-Z]{2}-\d{4}$
   */
  code: string;
  /**
   * @format email
   */
  email: string;
}

export interface InternalErrorDto {
  message: string;
  traceId: string | null;
}

export interface JacksonUserRequest {
  /**
   * @alias name, userName
   */
  user_name: string;
  bio?: string | null;
}

export interface JacksonUserResponse {
  user_name: string;
  bio?: string | null;
}

export interface NotFoundErrorDto {
  message: string;
  resource: string | null;
}

export interface Pageable {
  paged: boolean;
  unpaged: boolean;
  pageNumber: number;
  pageSize: number;
  offset: number;
  sort: Sort;
}

export interface SearchFilter {
  keyword: string;
  page: number;
  size: number;
}

export interface Sort {
  sorted: boolean;
  empty: boolean;
  unsorted: boolean;
}

export interface Tick {
  seq: number;
  timestamp: number;
}

export interface UpdateUserRequest {
  email: string | null;
  bio: string | null;
}

export interface UserDto {
  id: string;
  name: string;
}

export interface UserProfileDto {
  id: number;
  name: string;
  email: string;
  bio: string | null;
  address: Address | null;
}

export interface UserWithId {
  id: UserId;
  name: string;
}

export interface WrapperItem {
  id: string;
  label: string;
}

export interface ApiResponse<D, E> {
  data: D | null;
  error: E | null;
  success: boolean;
}

export interface Wrapper<T> {
  /**
   * @minLength 1
   * @maxLength 30
   */
  name: string;
  items: T[];
}

export interface domain_Page<T> {
}

export interface dto_Page<T> {
  content: T[];
  totalElements: number;
  page: number;
  size: number;
}

/* ──────────── Endpoint Error Aliases ──────────── */

export type ProtectedEndpointError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type AlwaysOkError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type CreateUserError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type SampleUserError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type CreateError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type ListError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type SearchError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type WhoamiError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type RequestAttrError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetItemWithMatrixError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetMonoError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetFluxError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetSuspendError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetCircleError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetSquareError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetTriangleError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type TicksError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type DownloadError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type SlowError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type FastError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type SubmitError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type WrappedError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetUserError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type EcValueClassCreateUserError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetOrderIdError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type GetUserProfileError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type UserCreateUserError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type UpdateUserError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type DeleteUserError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type ListUsersError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;
export type WrappedUserError = ApiError<BadRequestErrorDto | NotFoundErrorDto | InternalErrorDto>;

/* ──────────── API Functions ──────────── */

export interface ClientOptions {
  baseUrl?: string;
  timeoutMs?: number;
  authInterceptor?: (request: RequestInit) => RequestInit | Promise<RequestInit>;
  retry?: { maxAttempts: number; backoffMs: number; retryOn?: (status: number) => boolean };
}

export function createApi(options?: ClientOptions) {
  const _baseUrl = options?.baseUrl ?? "/api";
  return {
    ecAuthRetry: {
      /**
       * Returns 200 on even calls, 503 SERVICE_UNAVAILABLE on odd calls,
 so callers can verify retry behaviour.
       */
      protectedEndpoint: async (authorization: string, signal?: AbortSignal): Promise<EcAuthRetryResponse> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', headers: { 'Authorization': authorization }, signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-auth-retry/protected`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Always returns 200 — used to verify auth header is forwarded without retry.
       */
      alwaysOk: async (authorization: string, signal?: AbortSignal): Promise<EcAuthRetryResponse> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', headers: { 'Authorization': authorization }, signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-auth-retry/always-ok`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecJackson: {
      createUser: async (request: JacksonUserRequest, signal?: AbortSignal): Promise<JacksonUserResponse> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(request), signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-jackson/users`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA POST ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      sampleUser: async (signal?: AbortSignal): Promise<JacksonUserResponse> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-jackson/users/sample`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecJava: {
      get: async (name: string, signal?: AbortSignal): Promise<EcJavaDto> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-java/${encodeURIComponent(String(name))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      create: async (dto: EcJavaDto, signal?: AbortSignal): Promise<EcJavaDto> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(dto), signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-java`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA POST ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecPageable: {
      list: async (page?: number, size?: number, sort?: string, signal?: AbortSignal): Promise<domain_Page<EcPageableItem>> => {
        const params = new URLSearchParams();
        if (page !== undefined) params.append('page', String(page));
        if (size !== undefined) params.append('size', String(size));
        if (sort !== undefined) params.append('sort', String(sort));
        const qs = params.toString();
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec/pageable${qs ? `?${qs}` : ''}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecParameterKinds2: {
      /**
       * Endpoint using @ModelAttribute — DTO fields flattened into query string.
       */
      search: async (keyword: string, page: number, size: number, signal?: AbortSignal): Promise<string[]> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-param-kinds2/search?keyword=${encodeURIComponent(String(keyword))}&page=${encodeURIComponent(String(page))}&size=${encodeURIComponent(String(size))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Endpoint using @CookieValue — cookie value injected from request cookies.
       */
      whoami: async (cookies?: Record<string, string>, signal?: AbortSignal): Promise<string> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', headers: { 'Cookie': Object.entries(cookies ?? {}).map(([k, v]) => `${k}=${v}`).join('; ') }, signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-param-kinds2/whoami`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Endpoint using @RequestAttribute — server-side only attribute, excluded from SDK.
       */
      requestAttr: async (signal?: AbortSignal): Promise<string> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-param-kinds2/request-attr`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Endpoint using @MatrixVariable — key=value segments in path.
       */
      getItemWithMatrix: async (id: number, color: string, signal?: AbortSignal): Promise<string> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-param-kinds2/items/${encodeURIComponent(String(id))}?color=${encodeURIComponent(String(color))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecReactive: {
      getMono: async (signal?: AbortSignal): Promise<UserDto> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-reactive/mono`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      getFlux: async (signal?: AbortSignal): Promise<UserDto[]> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-reactive/flux`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      getSuspend: async (signal?: AbortSignal): Promise<UserDto> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-reactive/suspend`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecSealed: {
      getCircle: async (signal?: AbortSignal): Promise<Shape> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-sealed/shape/circle`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      getSquare: async (signal?: AbortSignal): Promise<Shape> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-sealed/shape/square`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      getTriangle: async (signal?: AbortSignal): Promise<Shape> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-sealed/shape/triangle`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecStreaming: {
      ticks: async (signal?: AbortSignal): Promise<AsyncIterable<Tick>> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-streaming/ticks`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      download: async (name: string, signal?: AbortSignal): Promise<Blob> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-streaming/file/${encodeURIComponent(String(name))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecTimeout: {
      /**
       * Sleeps for 1 second before responding — useful for testing timeoutMs + AbortSignal.
       */
      slow: async (signal?: AbortSignal): Promise<EcTimeoutResponse> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-timeout/slow`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Responds immediately — baseline for comparison.
       */
      fast: async (signal?: AbortSignal): Promise<EcTimeoutResponse> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-timeout/fast`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecValidation: {
      submit: async (body: EcValidationRequest, signal?: AbortSignal): Promise<EcValidationRequest> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body), signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/ec-validation/submit`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA POST ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      wrapped: async (signal?: AbortSignal): Promise<Wrapper<WrapperItem>> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/ec-validation/wrapped`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    ecValueClass: {
      getUser: async (id: string, signal?: AbortSignal): Promise<UserWithId> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-value-class/users/${encodeURIComponent(String(id))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      createUser: async (body: UserWithId, signal?: AbortSignal): Promise<UserWithId> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body), signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-value-class/users`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA POST ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      getOrderId: async (id: number, signal?: AbortSignal): Promise<OrderId> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/ec-value-class/orders/${encodeURIComponent(String(id))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
    user: {
      /**
       * Retrieve a user profile.
       */
      getUserProfile: async (id: number, signal?: AbortSignal): Promise<UserProfileDto> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/users/${encodeURIComponent(String(id))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Create a new user.
       */
      createUser: async (request: CreateUserRequest, signal?: AbortSignal): Promise<UserProfileDto> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(request), signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/users`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA POST ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Update an existing user.
       */
      updateUser: async (id: number, request: UpdateUserRequest, signal?: AbortSignal): Promise<UserProfileDto> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(request), signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/users/${encodeURIComponent(String(id))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA PUT ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Delete a user.
       */
      deleteUser: async (id: number, signal?: AbortSignal): Promise<void> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'DELETE', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/users/${encodeURIComponent(String(id))}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA DELETE ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return;
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * List users with pagination parameters.
       * @param {number} [page=0] Server default: 0
       * @param {number} [size=20] Server default: 20
       */
      listUsers: async (page?: number, size?: number, keyword?: string, signal?: AbortSignal): Promise<dto_Page<UserProfileDto>> => {
        const params = new URLSearchParams();
        if (page !== undefined) params.append('page', String(page));
        if (size !== undefined) params.append('size', String(size));
        if (keyword !== undefined) params.append('keyword', String(keyword));
        const qs = params.toString();
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/users/list${qs ? `?${qs}` : ''}`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

      /**
       * Return a user wrapped in a multi-parameter generic response envelope.
       */
      wrappedUser: async (id: number, signal?: AbortSignal): Promise<ApiResponse<UserProfileDto, string>> => {
        const __timeoutMs = options?.timeoutMs;
        const __controller = new AbortController();
        let __timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (__timeoutMs !== undefined) {
          __timeoutId = setTimeout(() => __controller.abort(new ApiTimeoutError()), __timeoutMs);
        }
        const __signals = [__controller.signal, ...(signal ? [signal] : [])];
        const __signal = __signals.length > 1 ? AbortSignal.any(__signals) : __controller.signal;
        const __maxAttempts = options?.retry?.maxAttempts ?? 0;
        const __backoffMs = options?.retry?.backoffMs ?? 200;
        const __retryOn = options?.retry?.retryOn ?? ((s: number) => s >= 500);
        let __attempt = 0;
        // eslint-disable-next-line no-constant-condition
        while (true) {
          let __req: RequestInit = { method: 'GET', signal: __signal };
          if (options?.authInterceptor) __req = await options.authInterceptor(__req);
          try {
            const res = await fetch(`${_baseUrl}/api/users/${encodeURIComponent(String(id))}/wrapped`, __req);
            if (!res.ok) {
              let __errBody: unknown = null;
              try { __errBody = await res.json(); } catch (_e1) {
                try { __errBody = await res.text(); } catch (_e2) { __errBody = null; }
              }
              throw new ApiError(res.status, __errBody, `SPIA GET ${res.url} failed: ${res.status} ${res.statusText}`);
            }
            clearTimeout(__timeoutId);
            return res.json();
          } catch (error) {
            if (error instanceof Error && error.name === 'AbortError') {
              clearTimeout(__timeoutId);
              throw new ApiTimeoutError();
            }
            if (error instanceof ApiError && __retryOn(error.status) && __attempt < __maxAttempts) {
              __attempt++;
              await new Promise(r => setTimeout(r, __backoffMs));
            } else {
              clearTimeout(__timeoutId);
              throw error;
            }
          }
        }
      },

    },
  };
}
